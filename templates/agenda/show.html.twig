{% extends 'base.html.twig' %}

{% block title -%}
    {{ event.title }} - Agenda
{%- endblock title %}

{% block body %}
    {#<div id="col-left" class="two-cols-content evenement">#}

        {# Breadcrumb #}
        {% include "_includes/breadcrumb.html.twig" %}

        <article class="contenu-page">
            <h1>{{ event.title }}</h1>

            {# Date #}
            {% if event.dateStart %}
                <span class="date">{{ event.dateStart|localizeddate('full', 'short')  }}</span>
            {% endif %}

            {# Affiche #}
            {% if event.picture and event.picture.webpath %}
                <div>
                    <a rel="prettyPhoto" href="{{ asset(event.picture.webpath) | imagine_filter('modal_img') }}"
                       class="thumbnail">
                        <img data-src="holder.js/160x120" alt="{{ event.picture.title }}"
                             src="{{ asset(event.picture.webpath) | imagine_filter('evenement_view') }}"
                             title="{{ event.picture.title }}"
                             class="imagecache imagecache-evenement_view" width="588"/>
                    </a>
                </div>
            {% endif %}

            <div><p>{{ event.description|raw }}</p></div>
        </article>

        {# Liste vidéos liées #}
        {% include "agenda/blocs/bloc_videos.html.twig" %}

{% endblock body %}

{% block columnRight %}

    {# Liste articles liés #}
    {% include "agenda/blocs/bloc_articles.html.twig" %}

    {# Liste des partenaires de l'évènement #}
    {% include "agenda/blocs/bloc_partenaires.html.twig" %}

{% endblock columnRight %}
