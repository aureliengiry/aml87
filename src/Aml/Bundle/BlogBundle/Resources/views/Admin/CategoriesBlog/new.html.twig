<h1>BlogCategories creation</h1>

<form action="{{ path('admin_content_blog_categories_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_content_blog_categories') }}">
            Back to the list
        </a>
    </li>
</ul>
