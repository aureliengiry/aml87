<h1>BlogCategories list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>System_name</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_content_blog_categories_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.systemname }}</td>
            <td>{{ entity.name }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_content_blog_categories_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_content_blog_categories_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_content_blog_categories_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
