{# Bloc Contact form #}
{% form_theme contact_form 'bootstrap_3_layout.html.twig' %}

<div id="contact-block">
    <h3>Formulaire de contact</h3>

    {{ form(contact_form, { attr: { id: 'container-form-contact-us', novalidate: '' } }) }}
</div>
